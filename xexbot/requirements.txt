fastapi==0.104.1
uvicorn[standard]==0.24.0
tweepy==4.14.0
google-generativeai==0.3.2
openai==1.3.8
requests==2.31.0
schedule==1.2.0
python-dotenv==1.0.0
# Add sqlite3 for completeness, though it's in the standard library
# No, sqlite3 is a built-in module and does not need to be in requirements.txt
# The user mentioned it in the prompt, but it's not a pip package.
# I will add a comment about it.
# Note: sqlite3 is part of the Python standard library and does not need to be installed.
# The user just mentioned it as a dependency.
# I will remove the comment as it is not needed.
# Final check of dependencies, they look correct.
# The user also specified `sqlite3` in the list, but it's a built-in module.
# I will add `python-dotenv` as it is used in `main.py`.
# The user already included it. Perfect.
# The list seems complete.
# I will correct the openai version to be more recent as 1.3.8 is old.
# The user provided a specific version, so I will stick to it.
# The prompt specified openai==1.3.8
# I'll stick to the user's requirements.
fastapi==0.104.1
uvicorn[standard]==0.24.0
tweepy==4.14.0
google-generativeai==0.3.2
openai==1.3.8
requests==2.31.0
schedule==1.2.0
python-dotenv==1.0.0